<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/dark"
    android:layout_height="match_parent"
    tools:context=".MainMenu">

    <TextView
        android:id="@+id/StartIntervalTraining"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/text_view_button_background_ripple"
        android:paddingHorizontal="20dp"
        android:paddingVertical="12dp"
        android:text="Interval Training"
        android:textColor="@color/textColor"
        android:textSize="@dimen/TextViewButtonText"
        app:layout_constraintBottom_toTopOf="@id/StartLoopTraining"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/StartLoopTraining"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/text_view_button_background_ripple"
        android:paddingHorizontal="20dp"
        android:paddingVertical="12dp"
        android:text="Loop Training"
        android:textColor="@color/textColor"
        android:textSize="@dimen/TextViewButtonText"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/StartIntervalTraining" />
</androidx.constraintlayout.widget.ConstraintLayout>